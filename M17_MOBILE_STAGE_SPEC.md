# V7 M17 · 移动端阶段条自动定位修复

## 问题

技能、专精、成长节点和职业能力页面使用横向阶段条。用户进入后续阶段，或点击“下一步”自动切换到未完成阶段时，阶段条仍停留在最左侧，当前阶段可能位于视口外。

## 修复规则

- 进入页面时恢复该阶段条上次的 `scrollLeft`。
- 恢复完成后检查当前激活按钮是否完全可见。
- 若激活按钮位于视口外，自动将其滚动到阶段条中央。
- 若激活按钮已经可见，不改变用户当前浏览位置。
- 点击后续阶段、点击“下一步”、完成前一阶段后自动进入下一阶段时均执行该逻辑。
- 开启“减少动态效果”时取消平滑动画。

## 覆盖区域

- 技能与专精阶段条。
- 职业能力选择组阶段条。
- 子职业与成长节点切换条。
- 逐级职业等级点位。
- 法术环阶和状态筛选条。

## 验收

1. 在技能第 1 阶段时手动将阶段条停留在左侧。
2. 点击底部“下一步”，切换到第 4 阶段。
3. 第 4 阶段按钮应自动出现在视口中部。
4. 用户手动拖动阶段条后，只要当前激活项仍可见，不应被重新居中。
5. 返回该页面时先恢复原位置；若当前激活项不可见，再进行最小必要滚动。
