import{abilities,classes,skills}from"../data/core.js";
import{feats}from"../data/progression.js";
import{spellData}from"../data/spells.js";
import{characterSummary,fmt,spellSources,spellChoice,skillBonus}from"../selectors.js";
import{navigate}from"../router.js";
import{lastActiveStep}from"../components/shell.js";
import{esc}from"../components/ui.js";
export function renderCharacter(build){const s=characterSummary(build),spellNames=new Map(spellData.map(x=>[x.key,x.name]));document.getElementById("app").innerHTML=`<main class="character-page"><header class="character-topbar"><button class="ghost-button" data-back-build>返回构筑</button><div><span class="kicker">CHARACTER SHEET</span><strong>${esc(build.name)}</strong></div><button class="secondary-button" data-edit-build>修改构筑</button></header><div class="character-content"><section class="character-identity"><div><span class="character-level">${build.targetLevel}</span><span>等级</span></div><div><h1>${esc(build.name)}</h1><p>${esc(build.identity.race)} · ${esc(s.route)}</p><small>${Object.entries(build.subclasses).map(([k,v])=>`${classes[k].name}：${v}`).join(" · ")||"尚未选择子职业"}</small></div><div class="character-combat"><div><span>生命</span><b>${s.hp}</b></div><div><span>护甲</span><b>${s.ac}</b></div><div><span>先攻</span><b>${fmt(s.initiative)}</b></div><div><span>法术 DC</span><b>${s.casting?.dc||"—"}</b></div></div></section><section class="sheet-section"><h2>属性</h2><div class="score-grid large">${abilities.map(a=>`<div><span>${a.name}</span><b>${s.scores[a.key]}</b><small>${fmt(Math.floor((s.scores[a.key]-10)/2))}</small></div>`).join("")}</div></section><div class="sheet-columns"><section class="sheet-section"><h2>技能</h2><div class="sheet-skill-list">${s.skills.map(key=>`<div><span>${skills.find(x=>x.key===key)?.name||key}${s.expertise.includes(key)?" · 专精":""}</span><b>${fmt(skillBonus(build,key))}</b></div>`).join("")||'<p class="muted">尚未选择技能。</p>'}</div></section><section class="sheet-section"><h2>专长与职业能力</h2><div class="chip-list">${build.feats.map(x=>`<span class="tag">${esc(feats[x.type]?.name||x.type)}</span>`).join("")||'<span class="muted">尚未选择专长</span>'}</div><div class="summary-list">${Object.entries(build.classChoices).map(([id,list])=>`<div class="summary-row"><span>${esc(id)}</span><b>${list.join("、")}</b></div>`).join("")||'<div class="summary-row"><span>主动能力</span><b>—</b></div>'}</div></section></div><section class="sheet-section"><h2>戏法与法术</h2><div class="spell-sheet-groups">${spellSources(build).map(src=>{const c=spellChoice(build,src.key),keys=[...c.cantrips,...(c.prepared.length?c.prepared:c.spells)];return`<div><h3>${src.name}</h3><div class="chip-list">${keys.map(k=>`<span class="tag">${esc(spellNames.get(k)||k)}</span>`).join("")||'<span class="muted">尚未选择</span>'}</div></div>`}).join("")||'<p class="muted">当前没有施法职业。</p>'}</div></section></div></main>`;document.querySelector("[data-back-build]").addEventListener("click",()=>history.length>1?history.back():navigate(`/build/${build.id}/${lastActiveStep(build)}`));document.querySelector("[data-edit-build]").addEventListener("click",()=>navigate(`/build/${build.id}/${lastActiveStep(build)}`))}
