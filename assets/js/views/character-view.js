import{abilities,classes,skills}from"../data/core.js";
import{feats}from"../data/progression.js";
import{spellData}from"../data/spells.js";
import{characterSummary,fmt,spellSources,spellChoice,skillBonus,activeFeatureGroups}from"../selectors.js";
import{spellEffectSummary,featureEffectSummary}from"../rules/effects.js";
import{effectFormulaHtml,featureEffectHtml}from"../components/effects-ui.js";
import{spellImageTag,bindImageFallbacks}from"../data/spell-assets.js";
import{navigate}from"../router.js";
import{lastActiveStep}from"../components/shell.js";
import{esc}from"../components/ui.js";
export function renderCharacter(build){const s=characterSummary(build),featureGroups=activeFeatureGroups(build).filter(group=>group.selected.length),spellGroups=spellSources(build).map(src=>{const choice=spellChoice(build,src.key),keys=[...choice.cantrips,...new Set(choice.prepared.length?choice.prepared:choice.spells)];return{src,choice,items:keys.map(key=>spellData.find(x=>x.key===key)).filter(Boolean)}});document.getElementById("app").innerHTML=`<main class="character-page"><header class="character-topbar"><button class="ghost-button" data-back-build>返回构筑</button><div><span class="kicker">CHARACTER SHEET</span><strong>${esc(build.name)}</strong></div><button class="secondary-button" data-edit-build>修改构筑</button></header><div class="character-content"><section class="character-identity"><div><span class="character-level">${build.targetLevel}</span><span>等级</span></div><div><h1>${esc(build.name)}</h1><p>${esc(build.identity.race)} · ${esc(s.route)}</p><small>${Object.entries(build.subclasses).map(([k,v])=>`${classes[k].name}：${v}`).join(" · ")||"尚未选择子职业"}</small></div><div class="character-combat"><div><span>生命</span><b>${s.hp}</b></div><div><span>护甲</span><b>${s.ac}</b></div><div><span>先攻</span><b>${fmt(s.initiative)}</b></div><div><span>法术 DC</span><b>${s.casting?.dc||"—"}</b></div></div></section><section class="sheet-section"><h2>属性</h2><div class="score-grid large">${abilities.map(a=>`<div><span>${a.name}</span><b>${s.scores[a.key]}</b><small>${fmt(Math.floor((s.scores[a.key]-10)/2))}</small></div>`).join("")}</div></section><div class="sheet-columns"><section class="sheet-section"><h2>技能</h2><div class="sheet-skill-list">${s.skills.map(key=>`<div><span>${skills.find(x=>x.key===key)?.name||key}${s.expertise.includes(key)?" · 专精":""}</span><b>${fmt(skillBonus(build,key))}</b></div>`).join("")||'<p class="muted">尚未选择技能。</p>'}</div></section><section class="sheet-section"><h2>专长</h2><div class="chip-list">${build.feats.map(x=>`<span class="tag">${esc(feats[x.type]?.name||x.type)}</span>`).join("")||'<span class="muted">尚未选择专长</span>'}</div></section></div><section class="sheet-section"><h2>职业能力</h2><div class="character-feature-groups">${featureGroups.map(group=>`<article><h3>${esc(classes[group.classKey]?.name||group.classKey)} · ${esc(group.title)}</h3>${group.selected.map(key=>{const option=group.options.find(x=>x[0]===key),effect=featureEffectSummary(build,group,key);return`<div class="character-feature-row"><span class="option-glyph">✦</span><div><b>${esc(option?.[1]||key)}</b><p>${esc(option?.[2]||"")}</p>${featureEffectHtml(effect)}</div></div>`}).join("")}</article>`).join("")||'<p class="muted">尚未选择主动职业能力。</p>'}</div></section><section class="sheet-section"><h2>戏法与法术</h2><div class="spell-sheet-groups">${spellGroups.map(({src,choice,items})=>`<div><h3>${esc(src.name)}</h3><div class="character-spell-cards">${items.map(sp=>{const prepared=choice.prepared.includes(sp.key),effect=spellEffectSummary(build,sp,src,sp.level);return`<article class="character-spell-card">${spellImageTag(sp,"summary-spell-image")}<div><b>${esc(sp.name)}${prepared?" · 已准备":""}</b><small>${sp.level?sp.level+"环":"戏法"} · ${esc(sp.school)}</small>${effectFormulaHtml(effect,{compact:true})}</div></article>`}).join("")||'<span class="muted">尚未选择</span>'}</div></div>`).join("")||'<p class="muted">当前没有施法职业。</p>'}</div></section></div></main>`;bindImageFallbacks();document.querySelector("[data-back-build]").addEventListener("click",()=>history.length>1?history.back():navigate(`/build/${build.id}/${lastActiveStep(build)}`));document.querySelector("[data-edit-build]").addEventListener("click",()=>navigate(`/build/${build.id}/${lastActiveStep(build)}`))}
