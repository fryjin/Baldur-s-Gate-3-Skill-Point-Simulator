import{classes,classMeta}from"../data/core.js";
import{shell,bindShell}from"../components/shell.js";
import{setLevelClass}from"../store.js";
import{levelClassLevel,levelUnlocks,routeBreakdown}from"../selectors.js";
let focusIndex=0;
export function renderRoute(build){focusIndex=Math.min(focusIndex,build.targetLevel-1);const key=build.levels[focusIndex],cl=classes[key],classLevel=levelClassLevel(build,focusIndex),unlocks=levelUnlocks(build,focusIndex);const view=`<div class="decision-view"><header class="view-heading"><div><span class="kicker">STEP 02</span><h1>逐级职业路线</h1><p>为目标等级内的每一级选择职业；各职业自身等级决定后续解锁。</p></div><strong class="view-progress">${routeBreakdown(build)}</strong></header><div class="decision-content"><div class="route-layout"><div class="route-track">${Array.from({length:12},(_,i)=>{const k=build.levels[i],enabled=i<build.targetLevel;return`<button class="route-node ${i===focusIndex?'active':''}" data-route-index="${i}" aria-disabled="${!enabled}"><b>${i+1}</b><span>${enabled&&k?classes[k].name:'—'}</span></button>`}).join('')}</div><div class="route-editor"><section class="route-editor-card"><span class="kicker">角色第 ${focusIndex+1} 级</span><h2>选择本级职业</h2><select id="route-class">${Object.entries(classes).map(([k,v])=>`<option value="${k}" ${k===key?'selected':''}>${v.name}</option>`).join('')}</select><div class="route-summary"><span class="tag">${cl.name}职业 ${classLevel}级</span><span class="tag">${classMeta[key]?.role||''}</span></div></section></div></div></div></div>`;const detail=`<span class="kicker">LEVEL ${focusIndex+1}</span><h2>${cl.name} ${classLevel}级</h2><p>${classMeta[key]?.role||''}</p><div class="detail-list"><div class="detail-row"><span>角色等级</span><b>${focusIndex+1}</b></div><div class="detail-row"><span>职业自身等级</span><b>${classLevel}</b></div><div class="detail-row"><span>子职业解锁</span><b>${cl.subclassAt}级</b></div></div><div class="detail-section"><h3>本级解锁</h3><div class="unlock-list">${(unlocks.length?unlocks:['本级没有必须选择的新节点']).map(x=>`<div class="unlock-item"><span class="unlock-dot"></span><span>${x}</span></div>`).join('')}</div></div>`;document.getElementById('app').innerHTML=shell(build,'route',view,detail);bindShell(build,'route');document.querySelectorAll('[data-route-index]').forEach(btn=>btn.addEventListener('click',()=>{if(btn.getAttribute('aria-disabled')==='true')return;focusIndex=Number(btn.dataset.routeIndex);renderRoute(build)}));document.getElementById('route-class').addEventListener('change',e=>setLevelClass(build.id,focusIndex,e.target.value));}
