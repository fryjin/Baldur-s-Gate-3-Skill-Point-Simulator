import{effectAverageLabel}from"../rules/effects.js";
import{esc}from"./ui.js";
export function effectFormulaHtml(effect,{compact=false}={}){if(!effect?.parts?.length)return"";return`<section class="computed-effect ${compact?"compact":""}"><div class="computed-effect-head"><span>${effect.kind==="healing"?"当前治疗":"当前效果公式"}</span><small>按当前属性计算</small></div><div class="effect-part-list">${effect.parts.map(part=>`<div class="effect-part damage-${part.typeInfo.css}"><span class="damage-type-icon" aria-hidden="true">${part.typeInfo.icon}</span><div><b>${esc(part.formula)}</b><span>${esc(part.label||part.typeInfo.label)}</span>${part.stats&&!compact?`<small>${esc(effectAverageLabel(part.stats))}</small>`:""}${part.alternate&&!compact?`<small>${esc(part.alternate)}</small>`:""}</div></div>`).join("")}</div>${effect.note&&!compact?`<p class="effect-note">${esc(effect.note)}</p>`:""}</section>`}
export function featureEffectHtml(effect){if(!effect)return"";if(effect.formula)return`<div class="feature-computed damage-${effect.typeInfo?.css||"force"}"><span class="damage-type-icon">${effect.typeInfo?.icon||"✦"}</span><div><b>${esc(effect.formula)}</b><span>${esc(effect.label||effect.typeInfo?.label||"当前效果")}</span></div></div>`;if(effect.bonus)return`<div class="feature-computed"><span class="damage-type-icon">＋</span><div><b>+${effect.bonus}</b><span>${esc(effect.label)}</span></div></div>`;if(effect.attackBonus)return`<div class="feature-computed"><span class="damage-type-icon">⌖</span><div><b>+${effect.attackBonus}</b><span>${esc(effect.label)}</span></div></div>`;if(effect.acBonus)return`<div class="feature-computed"><span class="damage-type-icon">⬡</span><div><b>+${effect.acBonus}</b><span>${esc(effect.label)}</span></div></div>`;if(effect.resistance)return`<div class="feature-computed damage-${effect.typeInfo?.css||"force"}"><span class="damage-type-icon">${effect.typeInfo?.icon||"◆"}</span><div><b>${esc(effect.typeInfo?.label||"")}抗性</b><span>${esc(effect.label)}</span></div></div>`;return`<div class="feature-effect-note">${esc(effect.label||"")}</div>`}
