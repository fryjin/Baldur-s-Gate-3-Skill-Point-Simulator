# V8 Design System

## 设计概念

**章节式构筑桌面**：不是传统后台，也不是把游戏截图照搬成网页。视觉保留 BG3 的暗色、羊皮金与幻想气氛，但交互遵循现代产品 UI：一屏只有一个主任务，辅助信息降级，结果实时反馈。

## 信息架构

一级流程只有三个：

1. 人物构筑
2. 装备构筑
3. 最终构筑

一级导航不和页面内筛选混用。装备章节和装备槽位属于当前任务内的二级导航。

## PC

- 左：阶段导航。
- 中：当前任务。
- 右：当前决策的最终效果。
- 不新增第四栏。
- 装备详情使用弹层，不永久占据第四列。

## Mobile

- 顶部三阶段导航。
- 当前章节 + 当前槽位 + 装备列表单列。
- 装备详情使用 Bottom Sheet。
- 不把 PC 右栏压缩到手机上。

## 密度规则

- 结果摘要优先使用负空间和分割线，不滥用卡片。
- 候选装备卡只承担：名称、稀有度、核心效果、构筑标签、推荐度和装备动作。
- 完整获取说明与比较放入详情层。

## 视觉 Tokens

- 背景：`#0a0907`
- 一级面：`#100f0c`
- 二级面：`#15130f`
- 主文字：`#f0eadc`
- 次文字：`#a89f8c`
- 金色：`#d2ad5b`
- 高亮金：`#f0d58b`
- 主操作红：`#b94b42`
- 完成绿：`#82b887`

## 交互原则

- 一屏一个主动作。
- 返回保留当前阶段、章节和槽位。
- 装备选择后即时更新结果，不增加额外“确认”步骤。
- 第一章 → 第二章 → 第三章是渐进升级，不清空此前选择。
- 详情是查看，不是导航。
