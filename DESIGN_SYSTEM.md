# V10 设计系统：Adventurer's Codex

## 产品与场景

- 产品：博德之门 3 角色构筑决策工具。
- 核心用户：需要边查规则、边比较路线、边确认当前等级结果的玩家。
- 页面单一任务：把“当前等级下可以做出的选择”组织清楚，并持续展示这些选择形成的最终角色纸。

## 视觉方向

**战术典籍 / Adventurer's Codex**：不是复刻游戏界面，而是借用角色纸、皮革装订、黄铜刻度和升级路径的语义。

- Ink 950 `#090807`：页面底色
- Leather 700 `#32221D`：工作区表面
- Oxblood 700 `#6F2624`：主要动作与选中状态
- Brass 500 `#C8A55F`：等级、强调和结构线
- Parchment 100 `#F1E5CF`：主文字
- Ash 400 `#B9AA98`：辅助文字

## 标志性元素

顶部“12 级命运轨迹”同时承担：

1. 当前角色等级反馈；
2. 1–12 级成长语义；
3. 页面最具辨识度的视觉元素。

## 信息架构

```text
顶部：构筑身份 + 12级轨迹 + 高频操作
左侧：7个真实决策步骤
中间：当前步骤的选择、说明与比较
右侧：持续更新的最终角色纸
移动端：步骤网格 + 单步内容 + 角色纸抽屉
```

## 交互规则

- 主要交互区域不小于 44px。
- 选中状态同时使用黄铜边框、左侧强调线和勾选文字，不只依赖颜色。
- 150–300ms 微交互；支持 `prefers-reduced-motion`。
- 右侧结果更新使用一次轻量脉冲，不改变布局尺寸。
- 默认标准密度，可切换紧凑密度并保存在浏览器。
- 移动端不强制三栏，最终结果使用可关闭抽屉。
