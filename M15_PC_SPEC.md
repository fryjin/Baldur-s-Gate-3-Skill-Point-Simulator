# V7 M15 PC 无横向交互收口

## 目标

PC 端不要求用户拖动横向滚动条，也不依赖 Shift + 滚轮浏览内容。所有信息采用自动换行、网格换行或纵向滚动。

## 调整范围

- 已选职业能力：两列换行，内容较多时纵向滚动。
- 已准备法术：图标自动换行，内容较多时纵向滚动。
- 子职业、专长、技能来源和职业能力节点：响应式网格换行。
- 目标等级和逐级职业轨迹：宽屏单行，紧凑 PC 两行显示。
- 法术环阶筛选：自动换行，不横向拖动。
- 平板宽度资料库导航：网格换行。

## PC 交互规范

1. 鼠标滚轮只承担纵向滚动。
2. 不显示横向滚动条。
3. 不要求按住 Shift 或拖动底部滚动条。
4. 内容超出区域时，优先换行；空间仍不足时使用纵向滚动。
5. 选择项的宽度由容器决定，不使用固定宽度制造横向轨道。
6. 移动端 M13 独立视图保持原有触控横向筛选，不受本轮影响。
