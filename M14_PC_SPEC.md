# V7 M14 PC 端结构性收口规格

## 目标

M14 不新增业务规则，只收口 PC 端布局、滚动和信息职责。移动端继续使用 M13 独立组件树。

## PC 宽度策略

- `>=1600px`：步骤流程 / 当前决策（列表 + 详情）/ 决策最终效果同时显示。
- `1280–1599px`：隐藏常驻结果栏，通过顶部“本步结果”按需打开；中间保留列表与详情。
- `768–1279px`：隐藏常驻结果栏；当前详情排列在当前列表下方。
- `<768px`：继续使用 M13 独立移动端产品层。

## 高度和滚动

- PC 页面整体固定为 `100dvh`，浏览器页面本身不纵向滚动。
- 允许滚动：步骤栏、选项列表、当前详情正文、结果正文。
- 已准备法术只横向滚动并隐藏原生滚动条。
- 当前法术的操作按钮固定在详情区域底部。

## 法术页

结构固定为：

1. 法术来源与搜索筛选
2. 选择数量
3. 已准备法术横向栏
4. 法术列表（占用剩余高度）

法术卡片固定为：名称状态 / 环阶学派动作 / 两行说明 / 效果公式。

## 步骤编号

左侧编号、标题 `STEP XX` 与底部 `XX / XX` 均读取同一个 `activeSteps` 动态列表。
