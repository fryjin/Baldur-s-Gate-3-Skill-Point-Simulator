# V7 M18 · 移动端法术工具区修复

## 问题

移动端法术页的搜索框与筛选胶囊采用半透明 sticky 容器。法术列表滚动到容器后方时，卡片文字会穿透显示，造成筛选区和第一张卡片重叠的视觉结果。

## 修复

1. 搜索和筛选合并为独立 `m-spell-controls` 区域。
2. 工具区使用不透明深色背景和 `isolation:isolate`。
3. 设置明确的最小高度、上下边界和阴影。
4. 法术列表保持正常文档流，并从工具区下方开始。
5. Sticky 时允许列表在背后滚动，但不允许内容穿透。
6. 搜索框字号固定为 16px，避免 iOS Safari 自动缩放。
7. 筛选按钮清除继承的伪元素，避免异常尖角或尾部装饰。

## 验收

- 搜索框与筛选按钮不重叠。
- 筛选按钮与第一张法术卡不重叠。
- 滚动时卡片文字不会透过工具区。
- iPhone 390×844 下筛选按钮可横向浏览。
- 底部操作栏和安全区不受影响。
