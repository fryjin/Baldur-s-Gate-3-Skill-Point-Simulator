# V7 M24 · 逐级法术学习展示收口升级包

M24 保留 M23 的逐级学习规则、学习历史、替换记录和旧存档迁移，只推翻并重做展示层。

## 解决的问题

M23 把法术位、学习分布、学习节点、节点说明、替换控制和法术列表同时塞进中间操作区，导致法术列表被严重压缩，并出现明显的信息堆叠。

M24 将页面职责重新划分：

- 中间区域只负责当前学习节点和法术选择；
- 右侧结果区域负责环阶分布、节点完成度和替换使用情况；
- 替换法术按需打开，不再常驻占用列表空间。

## PC 页面结构

```text
法术来源 / 搜索 / 环阶筛选
选择数量
一行法术规则摘要
轻量逐级学习节点
当前节点简报
法术列表（占据剩余高度）
```

### 轻量节点

学习节点改为自动换行网格：

```text
术士1 2/2 ✓
术士2 1/1 ✓
术士3 1/1 ✓
术士4 1/1 ✓
术士5 0/1
```

- 不使用横向滚动；
- 当前节点高亮；
- 已完成节点显示对勾；
- 当前节点说明压缩为一行；
- 选择完成后继续自动进入下一个未完成节点。

## 右侧结果

法术选择结果新增：

- 当前各环已学习数量；
- 学习节点完成数量；
- 每个职业等级的完成度与最高环阶；
- 已使用替换次数。

这些信息不再占据中间法术列表空间。

## 替换法术

“可选替换”改为轻量按钮。点击后打开详情抽屉，选择：

1. 使用哪个职业等级的替换机会；
2. 移除哪个已知法术；
3. 返回法术列表选择替换后的法术。

## 移动端

- 学习节点使用下拉选择器，不强行堆叠多个节点卡片；
- 当前节点简报保持可见；
- 学习分布在“本步结果”中查看；
- 法术列表继续使用 M19/M20 的独立滚动结构。

## 覆盖方式

将本升级包中的文件按原目录覆盖项目。

M24 是基于 M20 的累计升级包，包含 M21、M22、M23 所需的必要文件。

继续保留：

```text
assets/spells/*.png
assets/css/m19-mobile-spells.css
assets/css/m20-mobile-spell-cards.css
assets/js/patches/m19-mobile-spells.js
```

M24 的 `index.html` 只加载：

```text
assets/js/patches/m24-spell-learning.js
```

不要同时加载 M23 的 `m23-spell-learning.js`，否则会重复注入界面。
