# BG3 构筑规划器 V8.2.2 — 装备规则深化 + 最终属性联动

基线 `assets/v8/v82.js` Git blob SHA：

`e340701c664f137bc61cec17fbeb8b714ab6de51`

本包与 HD 图片补丁兼容，并把背景恢复 CSS 一并保留。

## 核心变化

- 装备属性变化继续传导到 HP / AC / 先攻 / 技能 / 豁免 / 每个施法来源 / 武器命中伤害。
- 修复 Clothing 错误关闭武僧/野蛮人无甲防御。
- 游侠骑士正确获得重甲熟练。
- 重甲头盔等非护甲槽熟练要求也会正确触发施法阻断。
- 唯一装备不能重复占用两个槽位；非法导入状态进入统一结构校验。
- 防御、箭术、决斗、双武器战斗、双持客与装备实时联动。
- 魔刃契约主手近战按 CHA 计算。
- 勇士与法术狙击手的重击阈值分离计算。
- 装备候选卡直接显示“装备后实际变化”，不再只有静态说明。
- 最终页新增最终豁免、四个武器位、每个施法来源、技能最终数值、精确效果、条件效果。
- ACT 1 / 2 / 3 显示继承装备后的实际 AC / HP / 先攻 / 命中 / 法术 DC 演进。

## 安装

把 ZIP 解压到仓库根目录，覆盖其中同名文件，然后执行：

```bash
python tools/apply-v82-core-linkage.py
```

脚本会先检查 `v82.js` 是否仍是本轮基线。若不是，会停止，不会强行覆盖。

然后：

```bash
node --check assets/v8/equipment-rules.js
node --check assets/v8/v82.js
node tools/validate-v82-equipment-rules.mjs
```

验证脚本覆盖：

1. 优雅布衣 + 武僧无甲防御
2. 战士防御战斗风格
3. 游侠骑士重甲熟练
4. 未熟练重甲头盔阻止施法
5. 箭术远程命中 +2
6. 魔刃契约使用 CHA
7. 敏捷装备传导到技能
8. 重复唯一戒指检测
9. 双手主手自动卸下副手

## 设计边界

当前 Catalog 只有文字、没有精确 `rules` 的效果仍不会被强行换算成数值。

例如：

- 专注时附伤
- 击杀后隐形
- 奥术敏锐
- 光耀宝珠
- 回荡
- 巨武器大师 / 神射手主动开关

这些继续进入“条件型效果 · 未默认折算”，避免伪精确。
