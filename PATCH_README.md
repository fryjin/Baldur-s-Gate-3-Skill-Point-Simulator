# V7 M19 移动端法术页交互补丁

## 覆盖方式

将压缩包内文件按原目录覆盖到项目根目录：

```text
index.html
assets/css/m19-mobile-spells.css
assets/js/patches/m19-mobile-spells.js
```

补丁基于 GitHub 当前 `main` 分支的 V7 M18 结构制作，不包含法术图片，不会覆盖：

```text
assets/spells/*.png
```

## 修改内容

- 删除移动端法术页重复的“当前结果”卡片。
- 删除常驻“已准备法术”图标栏；通过“已准备”筛选查看。
- 将职业来源、选择数量、搜索框和筛选按钮合并成唯一工具栏。
- 工具栏固定占据独立网格行，不再悬浮覆盖法术卡片。
- 法术列表成为页面唯一纵向滚动区域。
- 最后一页底部按钮调整为“上一步 / 角色纸 / 完成构筑”。
- 搜索、筛选、选择与准备法术的原有事件监听保持不变。

## 回退

删除新增的两个 M19 文件，并将 `index.html` 恢复到 M18 版本即可。
