# V8.2 产品与交互规格

## 1. 产品结构

V8.2 采用三个一级阶段，避免 V7 一直扩展 STEP 编号：

1. 人物构筑
2. 装备构筑
3. 最终构筑

PC 使用左侧阶段导航；移动端使用顶部阶段导航。人物构筑内部才展示二级步骤，避免一级导航和步骤导航混在同一层。

## 2. 人物构筑迁移

V8 不重新复制人物规则，而是直接使用 V7：

- Store：人物存档与 mutation；
- Selectors：属性、AC、HP、先攻、技能、法术、校验；
- Spell Learning：逐职业等级法术学习节点和替换；
- Resource Model：法术位、契约法术位、术法点、气点等。

V8 人物阶段共八步：

```text
等级 → 职业 → 成长 → 属性 → 技能 → 职业能力 → 法术 → 确认
```

每一步都直接修改 V7 build，对旧存档保持兼容。

## 3. 装备章节

装备章节使用差量存储：

```text
ACT 1 全量基础选择
ACT 2 = ACT 1 + 第二章变化
ACT 3 = ACT 2 + 第三章变化
```

删除装备也作为章节变化记录，因此可以表达“第二章开始不再使用某件装备”。

## 4. 装备槽位

```text
头部
披风
护甲
手套
鞋子
项链
戒指 1
戒指 2
近战主手
近战副手
远程主手
远程副手
```

### 冲突规则

- 双手近战武器 → 清空近战副手；
- 盾牌 → 近战副手；
- 默认双持要求两把 Light 近战武器；
- Dual Wielder 专长允许非双手的更重单手武器双持；
- 远程副手仅用于手弩；
- 非手弩远程主手 → 清空远程副手；
- 同一唯一装备移动到新槽位时，从旧槽位移除。

## 5. 装备结果分层

### 可直接计算

- 属性设定值/属性加值；
- HP；
- AC；
- 先攻；
- 法术 DC；
- 法术攻击；
- 通用攻击加值；
- 武器伤害加值；
- 豁免加值；
- 重击阈值。

### 条件效果

以下类型不会直接叠入常驻属性面板：

- 专注时附伤；
- 对受光照目标附伤；
- 奥术敏锐层数；
- 重击后触发；
- 击杀后触发；
- 每回合/每短休能力；
- 优势/劣势；
- 易伤光环；
- 装备授予主动技能。

它们以“条件型装备效果”单独展示。

## 6. 装备熟练

规则层结合当前人物：

- 职业；
- 种族；
- 子职业；
- 装备自身特殊熟练规则。

装备列表、详情和最终构筑都会显示熟练警告。

## 7. 装备数据库

### Schema

```js
{
  id,
  name,
  en,
  act,
  slotType,
  rarity,
  source,
  summary,
  tags,
  image,

  armor: {
    type,
    baseAC,
    dexCap,
    proficiency,
    selfProficient,
    uncappedDex
  },

  weapon: {
    type,
    damage,
    damageType,
    category,
    properties
  },

  rules: [],
  conditional: [],
  grants: [],
  proficiency,
  sourceType,
  quality
}
```

### 数据层策略

- `equipment-catalog.js`：人工校验的高价值装备；
- `equipment-generated.js`：外部完整数据导入层；
- ID 去重时人工 curated 数据优先；
- `tools/import-bg3forge-items.mjs`：导入工具；
- 未能可靠解析的复杂被动保留为 conditional 文本。

## 8. 最终构筑

最终页合并：

- 人物身份；
- 职业路线；
- 最终属性；
- HP / AC / 先攻 / 法术 DC / 法术攻击 / 重击；
- 职业资源；
- 技能与专精；
- 已选职业能力；
- 核心法术；
- 最终装备；
- 三章装备路线；
- 条件型装备效果；
- 人物/装备规则警告。

## 9. 移动端

移动端不压缩 PC 三栏：

- 一级阶段顶部切换；
- 人物二级步骤使用下拉；
- 所有主要触控按钮至少维持适合手指操作的高度；
- 装备候选单列；
- 详情使用 Bottom Sheet；
- 底部操作栏考虑 safe-area；
- 法术列表改为单列。
