# V7 M24 展示层收口规格

## 中间区域

### 保留

- 法术来源；
- 搜索；
- 环阶筛选；
- 戏法、已知、法术书、准备数量；
- 一行规则摘要；
- 轻量学习节点；
- 当前节点简报；
- 法术列表。

### 移除

- 大号法术位说明面板；
- 常驻学习分布大卡片；
- 常驻替换控制表单；
- 多行规则说明；
- 卡片内部重复的学习节点标签。

## 右侧结果区域

展示：

- 当前各环数量；
- 学习节点完成度；
- 各节点最高可学习环阶；
- 替换机会使用情况。

## 高度原则

- 节点区域自适应内容，不滚动；
- PC 节点自动换行，不产生横向轨道；
- 法术列表使用剩余高度并独立纵向滚动；
- 当前节点简报不超过一行；
- 警告信息不超过一行，完整内容通过标题提示查看。

## 移动端原则

- 节点列表切换为原生下拉选择；
- 不增加新的横向交互；
- 替换操作进入详情抽屉；
- 右侧结果内容通过移动端“本步结果”抽屉展示。
